import { FilterOutPipe } from './filter-out.pipe';

describe('FilterOutPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOutPipe();
    expect(pipe).toBeTruthy();
  });
});
